# Backend Environment Variables

# Server Configuration
NODE_ENV=development                    # Environment: development, production, test
PORT=3001                               # Backend server port (default: 3001)
FRONTEND_URL=http://localhost:3000      # Frontend URL for CORS configuration

# AWS Bedrock Configuration for Claude Models
AWS_REGION=us-east-1                    # AWS region for Bedrock service (default: us-east-1)

# AWS Credentials (choose one of the following options):

# Option 1: Direct credentials (not recommended for production)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Option 2: AWS Profile (recommended for local development)
# AWS_PROFILE=your_profile_name

# Option 3: IAM Role (recommended for production)
# No configuration needed - AWS SDK will automatically use IAM role

# Option 4: AWS CLI Configuration
# AWS_SDK_LOAD_CONFIG=1  # Enable loading from ~/.aws/config

# Note: The application will attempt to use credentials in this order:
# 1. Environment variables (AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY)
# 2. AWS Profile
# 3. IAM Role (when deployed on AWS infrastructure)
# 4. AWS CLI configuration

# Available Claude Models via AWS Bedrock:
# - anthropic.claude-3-5-sonnet-20241022-v2:0 (Primary - best for proposals)
# - anthropic.claude-3-5-sonnet-20240620-v1:0 (Fallback v1)
# - anthropic.claude-3-sonnet-20240229-v1:0 (Fallback)
# - anthropic.claude-3-haiku-20240307-v1:0 (Fast fallback)

# Optional: Session Token (for temporary credentials)
# AWS_SESSION_TOKEN=your_session_token_here

# File Upload Configuration (optional - has defaults)
# MAX_FILE_SIZE=10485760  # Maximum file size in bytes (default: 10MB)
# MAX_FILES=5             # Maximum number of files per upload (default: 5)

# Template Configuration (optional - has defaults)
# DEFAULT_TEMPLATE=default  # Default template ID to use
# TEMPLATE_DIR=config/templates  # Directory containing templates

# Logging Configuration (optional)
# LOG_LEVEL=info  # Logging level: error, warn, info, debug

# Security Configuration (optional)
# RATE_LIMIT_WINDOW_MS=900000  # Rate limit window in ms (default: 15 minutes)
# RATE_LIMIT_MAX_REQUESTS=100  # Max requests per window (default: 100)